/**
   Definiations for the base template part of the library
 */
#include <unitLib_templates.h>
#include <iostream>

// template<typename T>
// friend std::ostream &operator <<(std::ostream& os, const Base<T>& value){
//     return os << value->get_value();
// }
